## 1. Advancement Definitions

- [x] 1.1 Add optional bounded advancement metadata to stage codec/data with explicit target realm/stage, ordinary/bottleneck kind, duration, requirement, success cost, and interruption loss.
- [x] 1.2 Ship exact rules qi-sensed-to-I `100/10/5/0`, I-to-II `100/20/10/0`, II-to-III `120/30/15/0`, and III-to-IV `200/80/30/5`; leave Qi IV+ without cap/rule.
- [x] 1.3 Add codec/reference/bound/kind/self-target/mismatched-target tests and current-`RegistryAccess` validation without order/id inference.

## 2. Protocol And Session Extension

- [x] 2.1 Extend the same bounded intent only here with `START_BREAKTHROUGH`, preserving V/B/G and adding configurable N without target/result/profile fields.
- [x] 2.2 Extend the existing UUID-keyed manager with ordinary/bottleneck advancement states, mutual exclusion, exact duration counting, and no persistent session data.
- [x] 2.3 Implement all start gates and final-tick revalidation from server profile/registry/session state with controlled rejection reasons.

## 3. Interruption And Transition Transactions

- [x] 3.1 Route the complete meditation interruption set plus G through idempotent advancement cancellation while allowing yaw/pitch.
- [x] 3.2 Apply zero ordinary loss, exact five-point Qi-III player/world loss, and zero clean-stop/reload invalidation loss through at most one immutable service replacement.
- [x] 3.3 Implement deterministic one-stage success with progress zero, exact stability cost, complete unrelated-v2 preservation, one final snapshot, no overflow/randomness/chaining.

## 4. Presentation Documentation And Validation

- [x] 4.1 Add transition-only clientbound status/cache and extend read-only H with kind, duration, requirement/cost, state, and Qi-IV release ceiling.
- [x] 4.2 Add exact-matrix/gate/timing/interruption/idempotence/atomicity/preservation/protocol/release-limit tests and a focused advancement validator.
- [x] 4.3 Add the indexed playable-loop KB note and update README, KB/index, AGENTS probes/acceptance, commands/keys, configuration, and pass/fail/`not_verified` ledgers across all five changes.
- [x] 4.4 Run strict validation for all five changes and implemented baseline, all affected Python validators, focused/Gradle tests, build, jar inspection, and bounded acceptance-server smoke.

## 5. Shared Feature Release

- [x] 5.1 As the final owner-approved serial integration task, update `gradle.properties`, `neoforge.mods.toml`, README jar-name examples, and `CHANGELOG.md` together exactly once to the shared `0.24.0` feature release, incorporating release notes from all five changes and performing no earlier duplicate bump.
