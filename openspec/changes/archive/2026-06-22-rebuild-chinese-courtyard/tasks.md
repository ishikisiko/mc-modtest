## 1. Form vocabulary (Step 1 — built form)

- [x] 1.1 Capture a baseline: regenerate the current `chinese_courtyard_001..006.nbt` library and record NBT hashes + silhouette scores for regression comparison after the rebuild.
- [x] 1.2 In `tools/buildgen/ops.py`, register four new roof handlers in `ROOF_REGISTRY`: `chinese_flush_gable` (硬山, gable flush with side wall), `chinese_overhang_gable` (悬山, overhang past gable wall), `chinese_half_hip` (歇山, upper gable + lower 45° 抱厦 skirt + 围脊), `chinese_round_ridge` (卷棚, no ridge block, smooth curve of partial blocks).
- [x] 1.3 Add a private `_chinese_roof_helpers` module (or section in `ops.py`) shared by the four handlers: eave curve geometry, ridge cap, 抱厦 skirt matrix, 围脊 block placement. No string-match dispatch between the four handlers.
- [x] 1.4 Delete `tools/buildgen/archetypes.py:_apply_chinese_roof` and its 1/2/1 overhang lie; route `roof_grade` for Chinese courtyard buildings through the new handlers via the form registry.
- [x] 1.5 Add `PLATFORM_STONE` and `COLUMN` slot entries to `tools/buildgen/styles/chinese_courtyard.json` with vanilla-only fallback lists (e.g. `minecraft:stone_bricks` / `minecraft:polished_andesite` for PLATFORM_STONE; `minecraft:stripped_dark_oak_log` / `minecraft:oak_fence` for COLUMN).
- [x] 1.6 Update `chinese_courtyard.json` `allowed_roof_types` to list only the four `chinese_*` forms; remove any cultivation monumental forms if present.
- [x] 1.7 Retune `chinese_courtyard.json` `proportions` for vernacular scale (roof ratio 0.4–0.55, platform height matching the `platform_tier` axis values).
- [x] 1.8 Rewrite `build_main_hall` (`archetypes.py:1260`) onto the cultivation massing grammar: raise a `PLATFORM_STONE` 台基 of the configured height, add standoff `COLUMN` 檐廊 on the entry face, and a 3/5/7-bay interior zone pattern (明间 / 次间 / 梢间 / 尽间). Footprint width SHALL follow the `main_bays` axis.
- [x] 1.9 Rewrite `build_side_wing` / `build_front_row` / `build_gate_house` to use the same platform + standoff-column + bay-zone pattern at smaller scale; only `main_hall` is 2 stories.
- [x] 1.10 Add a plaque binding for the `main_hall` archetype in `src/main/resources/data/myvillage/plaque_bindings.json`: pick a vernacular/civic-tier preset (`lord_manor_heraldry_5w` or `sect_simple_pine_4w`) and a 堂/斋-style inscription pool.
- [x] 1.11 Regenerate the 6 NBTs with the OLD layout still in place (form change only). Diff previews; confirm silhouettes now read as Chinese, not "Chinese-skinned manor".

## 2. Layout rebuild (Step 2 — plan)

- [x] 2.1 Extend `CompoundVariant` (`compound.py:49`) with new fields: `layout_type` (`standard` / `three-sided` / `mu`), `main_orientation` (`south` / `east` / `north`), `main_bays` (`3` / `5` / `7`), `platform_tier` (`none` / `stone_2` / `xumi_3`), `gate_type` (`guangliang` / `manzi` / `jinzhu`). Rename old `gate_style` to `gate_type` with the new value set. Update `to_dict` and `key`.
- [x] 2.2 Replace `select_variant` (`compound.py:183`) with a deterministic 6-row template table. Each row pins a distinct `(layout_type, main_orientation, main_bays, roof_grade, platform_tier, gate_type)` tuple; `seed % 6` picks the row. Minor axes (`water_form`, `planting_layout`) stay RNG-derived.
- [x] 2.3 Refactor `generate_compound` to split the lot into two z-bands (outer yard / main yard) plus a 垂花门 band between them, via a new `_compute_yard_bands(layout_type, lot_d)` helper. `CompoundGraph.meta` gains `outer_yard_band` and `main_yard_band` `(z0, z1)` tuples.
- [x] 2.4 Add a `_layout_outer_yard` step: place 影壁 (screen wall) parcel node inside the street gate on the central axis; place `front_row` (倒座) unless `layout_type == "three-sided"`. (Realized: `standard` carries 倒座; `three-sided` and the narrow `mu` band omit it.)
- [x] 2.5 Add a `_layout_inner_gate` step: place 垂花门 (inner gate) parcel node between the outer and main yard bands on the central axis, with the 垂莲柱 corner detail.
- [x] 2.6 Add a `_layout_main_yard` step: place two `side_wing` buildings east and west; place `main_hall` on the inward end of the central axis; place 月台 (moon platform) apron in front of the main hall.
- [x] 2.7 Replace `_route_circulation`'s side-corridor ground-path BFS with `_place_covered_galleries`: emit two `covered_gallery` parcel nodes (east and west), each 3-wide × 3-tall with standoff columns + roof tying into the 垂花门 and main hall eave lines. Reuse the geometry pattern from `sect.py:_place_sect_link`'s gallery code. (`generate_compound` now routes the chinese path through `_place_covered_galleries` + `_route_central_path`; the shared `_route_circulation` is kept for the cultivation small-courtyard / town / sect paths to preserve their byte-stable output.)
- [x] 2.8 Add a `_dress_main_yard` step: place a `courtyard_tree` parcel node (large deciduous / fruit variant, offset from the central axis) and optional `water_jar` nodes at the yard corners. (Tree on the main-yard plinth with dark_oak foliage; the ground-layer water/planting/jar sit in the open outer yard since the raised main yard has no ground-water layer.)
- [x] 2.9 Rewrite `_add_perimeter` to add a wall cap ridge (cap blocks along the top, not a single slab) and 墙垛 piers at each external corner and at regular intervals along long runs; optional 漏窗 cutouts. Ensure the wall extends down to the original ground where it meets the raised main-yard platform (no floating cells). (New `_add_chinese_perimeter`; the shared `_add_perimeter` is left untouched so cultivation/town/sect walls stay byte-stable.)
- [x] 2.10 Honor `main_orientation`: route the `gate_side` meta and the `main_hall` placement to the correct lot edge for `south` / `east` / `north`. (Field is first-class and the layout is parametric by `gate_side`; `south` is realized. `north` / `east` require a voxel-frame transform — a naive mirror breaks directional blockstates and the validator is south-only — so they are gated by `_resolve_gate_side` and deferred; all six shipped rows are `south`. Tracked for the follow-up per design Open Questions / deferred roadmap §E.)
- [x] 2.11 Regenerate the 6 NBTs with the new layout. Diff previews; confirm the outer/main yard split, 影壁, 垂花门, 抄手游廊, and 月台 read clearly. (6/6 regenerate and pass `validate_compound_library` full-profile; structural readout confirms gate → 影壁 → 外院/倒座 → 垂花门 → 厢房 + 月台 + 正房 with two 抄手游廊.)

## 3. Validation and acceptance

- [x] 3.1 Extend `validate_compound` (`compound.py:1222`) with: exactly one `inner_gate` between outer and main yards; `screen_wall` placed inside the street gate on the central axis; exactly two `covered_gallery` nodes (east + west) connecting 垂花门 flanks to main hall flanks; `moon_platform` adjacent to the main hall; `courtyard_tree` not overlapping any building/gallery/月台/water/planting cell; no `perimeter_wall` cell has air below it.
- [x] 3.2 Add a roof-closure check: every `chinese_half_hip` roof plane is closed at the gable-to-skirt seam (no roof-hole defect class, per the `fix-side-wall-cleanup` lesson).
- [x] 3.3 Add a `validate_compound_library` acceptance rule: the 6 shipped NBTs differ on `silhouette_score` by ≥ 15 within the library; no two NBTs are byte-identical.
- [x] 3.4 Add a regression guard: `cultivation_*` and `medieval_*` libraries stay byte-stable (they never invoke `chinese_*` roof forms or the new `PLATFORM_STONE`/`COLUMN` slots in the `chinese_courtyard` style).
- [x] 3.5 Confirm vanilla-profile `chinese_courtyard` output resolves every new slot to its `minecraft:` fallback (no air, no external-mod-only dependency).
- [x] 3.6 Regenerate `reports/compound_library_report.json` and `reports/compound_library_validation.json`; sanity-check the variant distribution and silhouette scores.
- [x] 3.7 Regenerate `out/preview/chinese_courtyard_001..006/` (slices, isometric, viewer); confirm aggregate `out/preview/index.html` is up to date.

## 4. Docs, version, and staged acceptance

- [x] 4.1 Update `docs/ai-kb/10_civic_family.md` with a "Courtyard family rebuild" section: form rebuild (4 new roofs + 台基 + 檐廊), layout rebuild (outer/main yard + 影壁 + 垂花门 + 抄手游廊 + 月台), and the new variant axes. See-also to `courtyard-compound`, `chinese-vernacular-roof-vocabulary`, `style-profile`.
- [x] 4.2 Enrich the §E (Courtyard-form expansions) entry in `docs/ai-kb/14_deferred_roadmap.md` with the explored 二进/三进/花园 design from this change's exploration (z-band `jin_count` abstraction, back_chamber / garden_rockery / free-curve pond, deferred follow-up). Mark "一进 rebuild" as ✅ realized by change `rebuild-chinese-courtyard`.
- [x] 4.3 Note in `AGENTS.md` that the `chinese_courtyard` family now uses the same massing slots as `cultivation_sect` (PLATFORM_STONE / COLUMN), and that adding new vernacular roof forms goes through the `ROOF_REGISTRY` (no string-prefix dispatch).
- [x] 4.4 Bump the mod version per the `openspec/config.yaml` rule — large feature `0.14.x → 0.15.0` — updating `gradle.properties`, `src/main/resources/META-INF/neoforge.mods.toml`, README jar-name examples, and `CHANGELOG.md` together. Note the `chinese_courtyard_NNN.nbt` regeneration as a breaking change in `CHANGELOG.md`.
- [x] 4.5 Run the validation checklist from `docs/ai-kb/09_validation_checklist.md` (Acceptance / preview command checklist): generate, validate, preview, build the jar.
- [ ] 4.6 Staged manual acceptance: place ≥2 of the rebuilt `chinese_courtyard_*` NBTs in-game and inspect (a) the silhouette reads 四合院 not "manor", (b) outer/main yard split with 影壁 blocking the sightline, (c) 垂花门 between yards, (d) 抄手游廊 connect 垂花门 to main hall, (e) main hall 台基 + 月台 + central-bay plaque, (f) the 6 NBTs visibly differ on plan and roofline. Capture screenshots; serve the preview aggregate over HTTP per `AGENTS.md` and report the host URL.
