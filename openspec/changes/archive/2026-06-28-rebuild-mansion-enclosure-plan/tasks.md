## 1. Orientation mechanism — door-wall relocation (`building-orientation-variants` spec)

- [x] 1.1 In `tools/buildgen/archetypes.py`, extend `_door(graph, main, rng, wall="front", avoid=None)` to accept a `wall ∈ {front, back, west, east}` argument. For `wall != "front"`, pick the door position along the wall's long axis (z for west/east walls; x for back), honor the existing `avoid` reservation, and record `graph.meta["door"] = {"volume": main.id, "wall": wall, "pos": <pos>}`.
      - **Implemented**: `_door` signature is `(graph, main, rng, avoid=None, wall="front")`; records `graph.meta["door"] = {"volume", "wall", "along"}` (key named `along`, not `pos`, since it is the along-axis door coordinate — used everywhere downstream).
- [x] 1.2 Thread a `facing ∈ {south, north, east, west}` (default `south`) through `generate_subbuilding` → `BuildContext` → the archetype builders. Default `south` MUST reproduce byte-identical output (regression guard).
      - **Implemented**: `form_overrides={"facing": ...}` flows `generate_subbuilding` → `massing_pass` → `build_massing` → builder; `FACING_TO_WALL = {"south":"front","north":"back","east":"east","west":"west"}` maps it. Byte-identity of the default-`south` path covered by task 6.2.
- [x] 1.3 In `tools/buildgen/passes.py` `facade_detail_pass`, read `graph.meta["door"]["wall"]` and carve the doorway on the selected wall (extend the `ops.doorway` call site; it already accepts a wall argument).
      - **Implemented**: `door["along"]` (renamed from `door["x"]` via replace_all across passes/facade) carries the along-axis coord; `ops.doorway` already dispatches on `wall` via `wall_pos`.
- [x] 1.4 Compute `door_info["front"]` from the door wall's outward direction: south→`(door_x, y, z0-1)`; north→`(door_x, y, z1+1)`; west→`(x0-1, y, door_z)`; east→`(x1+1, y, door_z)`. Extend the existing front-cell arithmetic in `archetypes.py`/`passes.py`.
      - **Implemented**: `_door` returns the `front` cell via `ops.wall_pos(vol, wall, along, y, depth_offset=1)` for all 4 walls; `door_info["front"]` is shifted into compound coords by `_translate_context`.
- [x] 1.5 Unit-test: build a `side_wing` with `facing=west`; assert the door is on the west wall, `door_info["front"]` is at `(x0-1, y, door_z)`, and the volume/footprint/`roof_axis` are identical to a `south`-facing wing. Build a `front_row` with `facing=north`; assert the door is on the back wall.
      - **Implemented**: `tools/buildgen/tests/test_mansion_enclosure_plan.py` checks `side_wing` west-facing door wall/front cell plus unchanged volume size and `roof_axis="z"`, and checks `front_row` north-facing uses the back wall.

## 2. Gate-house entrance (`mansion-gate-house` spec)

- [x] 2.1 In `tools/buildgen/compound.py`, add a `_place_gate_house(compound, style, axis, gate_type)` that builds a `gate_house` sub-building (existing `build_gate_house`) and places it straddling the south perimeter (south wall on z=0 line, body projecting inward), centered on the axis. Record a `gate_house` `BuildingSlot` with `facing=inward`.
      - **Implemented as part of the enclosure manifest** rather than a standalone `_place_gate_house`: the manifest entry for `gate_house` is realized first inside `_realize_mansion_enclosure` (its real depth must be measured before `gate_inner_z` can be derived). Equivalent behavior, different factoring.
- [x] 2.2 Modify `_add_chinese_perimeter` to accept the gate_house footprint and gap the south-perimeter wall to match it exactly, so the gate_house side walls seal the gap. Remove the carved-air `gate` set for the mansion path (keep it for the unchanged `chinese_courtyard` path).
      - **Implemented**: `_add_chinese_perimeter(..., gate_house_gap=(x0, x1))` gaps the south wall across `[x0,x1]`; the `chinese_courtyard` path still calls it without the gap (unchanged).
- [x] 2.3 Wire `_place_gate_house` into `generate_mansion` as the first enclosure step (replaces the implicit hole-in-wall gate). The player must walk through the gate_house to reach the 前院.
      - **Implemented**: `generate_mansion` → `_add_chinese_perimeter(gate_house_gap=...)` → `_realize_mansion_enclosure` (places gate_house first). All 6 variants: `_voxel_walk_bfs` reaches 前院 through the gate_house (`voxel_unreachable_door` no longer fires).
- [x] 2.4 Unit-test: generate a mansion; assert a `gate_house` slot straddles z=0, the perimeter is sealed except through it, and `_voxel_walk_bfs` reaches the 前院 by passing through the gate_house.
      - **Implemented**: `test_mansion_enclosure_plan.py` generates all 6 canonical variants, asserts `gate_house` footprints straddle z=0 and center on the axis, the south perimeter gap exactly matches the gate_house z=0 footprint, and `_voxel_walk_bfs` reaches the recorded `gate_inner_z` passage. The test caught a real gap-width defect; fixed by making `gate_house` accept explicit `footprint` overrides and binding the perimeter gap to the same `gate_type` footprint.

## 3. Enclosure planner (`compound-enclosure-planning` spec)

- [x] 3.1 In `tools/buildgen/compound.py`, add a `_plan_mansion_enclosure(variant, lot_w, lot_d, axis, seed) -> List[Placement]` returning the placement manifest: each `Placement = (archetype, facing, anchor_wall, offset_along_wall, importance)`, encoding the form rule (正房-south/倒座-north/西厢-east/东厢-west/gate_house-inward) with anchor walls per design D3.
      - **Implemented**: `_plan_mansion_enclosure(variant, lot_w, lot_d, axis) -> (List[_EnclosurePlacement], gates, gate_inner_z)`. `_EnclosurePlacement` carries `role/archetype/facing/anchor/slot_id/x0/z0/plinth_h/extra_overrides` (concrete origin rather than `offset_along_wall`, since the form rule + axis determine the position deterministically).
- [x] 3.2 Add `_derive_yards(compound, manifest) -> Dict[str, Set[Cell2]]` computing each 进 (前院/主院/后院/花园) as the enclosed negative space of its facing-buildings (flood-fill the interior cells not under any building footprint, partitioned by inner-gate boundaries).
      - **Implemented** as `_derive_mansion_yards`. The compound `meta` carries `*_yard_band` tuples consumed by `_cell_band` / `_natural_surface_y` / `_collect_path_endpoints`.
- [x] 3.3 Add `_realize_manifest(compound, style, manifest, contexts)` placing each building against its anchor wall with its form-rule facing via `_translate_context` (using the facing-aware `door_info` from task 1). Insert inner gates (仪门/二门) at the derived yard-adjacency boundaries.
      - **Implemented** as `_realize_mansion_enclosure` (places gate_house + 倒座 first to measure the real south-line depth, derives `gate_inner_z`, then the rest; then fills 主院 plinth, places 照壁, inserts 仪门/二门).
- [x] 3.4 Replace the body of `generate_mansion`'s layout calls (`_layout_front_yard`/`_layout_main_yard_mansion`/`_layout_back_yard`) with: `_plan_mansion_enclosure` → `_realize_manifest` → `_derive_yards`. Keep `_layout_garden` (garden realization unchanged). Keep `_place_yard_ground`, `_route_complete_path`, `_place_band_transition_stairs` as post-realization passes.
      - **Implemented**: `generate_mansion` body rewritten to plan → perimeter(gap) → realize → `_place_yard_ground` → `_layout_garden` → `_route_complete_path` → `_place_band_transition_stairs`.
- [x] 3.5 Unit-test: generate all 6 mansion variants; assert each 进 is a contiguous enclosed region with no building footprint cells; assert 仪门 borders 前院+主院 and 二门 borders 主院+后院 (by adjacency, not z-band).
      - **Implemented**: `test_mansion_enclosure_plan.py` derives front/main/back yards for all 6 canonical variants, checks each region is non-empty, 4-neighbour contiguous, and building-footprint-free, then verifies `yimen_gate` borders 前院+主院 and `ermen_gate` borders 主院+后院 by cell adjacency.

## 4. Path-as-input routing (`compound-enclosure-planning` spec, D4)

- [x] 4.1 After manifest realization, collect every `BuildingSlot.door_info["front"]` cell as a mandatory path endpoint (in addition to the existing `_collect_path_endpoints` landscape endpoints).
      - **Implemented**: `_collect_path_endpoints` already seeds every `door_info["front"]` as an endpoint; the mansion entry seeds `(axis, gate_inner_z)` instead of the legacy `(axis, z0+1)` when `gate_inner_z` is present.
- [x] 4.2 Route the gravel backbone in the derived yard space from the gate-house inner opening to every door endpoint. Reuse the existing single-source BFS `_route_complete_path` but seed it with the door endpoints and constrain it to the derived-yard cell set.
      - **Implemented**: `_route_complete_path` unchanged in structure; the gate entry is now the gate_house inner opening (`gate_inner_z`), backbone traced back to it via `_single_source_bfs_pred`. (Not constrained to a derived-yard set — the building-footprint + landscape `blocked` set already keeps the backbone in walkable yard space.)
- [x] 4.3 Wire `_voxel_walk_bfs` as the acceptance gate in `_realize_manifest`: if any door-cell is unreachable, raise `EnclosurePlanRejected` and the planner retries (in practice never triggers for a well-formed manifest). Document that the derived yard is contiguous by construction.
      - **Implemented differently (design deviation)**: no `EnclosurePlanRejected` retry. Instead the acceptance gate is `validate_mansion`'s `_voxel_walk_bfs` check (any `voxel_unreachable_door`/`voxel_unreachable_endpoint` = library build fails). Contiguity is guaranteed by construction: (a) deterministic `zlib.crc32` slot seed, (b) front_row footprint constrained to fit beside the gate_house, (c) `gate_inner_z` derived from the max south-line depth, (d) tower_house facing flipped to north so its porch no longer crosses the 二门. Retry was rejected because the manifest is fully deterministic — there is no randomization to retry against.

## 5. Validator rewrite (`validation` spec delta)

- [x] 5.1 Rewrite `validate_mansion` to add the enclosure invariants: `gate_house_missing` (no gate_house slot straddling south), `enclosure_facing_violation:<slot>` (facing ≠ form rule), `door_off_path:<slot>` (door-cell not on path), and 进-sequence adjacency (仪门 borders 前院+主院; 二门 borders 主院+后院) via derived-yard adjacency, NOT z-band comparison.
      - **Implemented**: added `MANSION_FORM_FACING` + `WALL_TO_FACING` (form rule: 正房 south / 倒座 north / 西厢 east / 东厢 west / gate_house+楼阁 north) and an enclosure-invariant block in `validate_mansion`. Facing read from `slot.door_info["wall"]`; 进-adjacency derived via `_derive_mansion_yards` (gates reconstructed from the `inner_gate` nodes' `band`/`kind`) with a 4-neighbour border test (`jin_sequence_violation:{yimen,ermen}`). `door_off_path` treats the front cell as on-path if it or a 4-neighbour is a path cell (the backbone stops one cell short by design). All 4 negative paths fire the right codes; all 6 variants pass with `door_reachable_rate=1.0`.
- [x] 5.2 Keep all grid-only checks in `validate_mansion` verbatim (perimeter floats, ground holes, voxel-walkability error codes, silhouette). Remove the z-band-coupled mansion checks (the old `oy_band < ig_band < my_band` style comparisons if any).
      - **Implemented**: all grid-only checks left verbatim. There were no `oy_band < ig_band < my_band`-style comparisons in `validate_mansion` to remove (those live only in `validate_compound`); the new 进-sequence check uses derived-yard adjacency, not band tuples.
- [x] 5.3 Add `facing_per_slot` map and `door_reachable_rate` stat to the mansion report.
      - **Implemented**: `stats["facing_per_slot"]` (slot_id → facing from the recorded door wall) and `stats["door_reachable_rate"]` (rounded) added to the mansion report.
- [x] 5.4 Leave `validate_compound` (band-coupled, governs `chinese_courtyard`) **unchanged**. Confirm `validate_small_courtyard` is unchanged.
      - **Confirmed**: only `validate_mansion` (+ two new module constants before it) was edited; `validate_compound` / `validate_small_courtyard` untouched.

## 6. Byte-stability guard + regression

- [x] 6.1 Extend the byte-stability guard in `tools/buildgen/tests/test_chinese_courtyard_regression.py` (or add a sibling test) to assert `chinese_courtyard_*`, `cultivation_sect_*`, `cultivation_town_*`, `medieval_*` NBT SHA-256 are unchanged by this change (they must not be regenerated).
      - **Implemented**: `_legacy_files()` now globs `chinese_courtyard_* + cultivation_town_* + cultivation_sect_* + medieval_*`; `chinese_mansion_*` excluded (regenerated by task 7.1). Guard passes: **59 NBTs unchanged**.
      - **Regression caught + fixed**: the guard surfaced that the skeleton rewrite (tasks 1–4) had regressed `chinese_courtyard` (6/6 failing `ground_kind_mismatch:open_sky_has_eave_block`, baseline passed) — violating D5. Root cause: `_place_yard_ground` added a `platform_open` skip for the mansion 主院 plinth that also skipped the courtyard plinth, leaving its open-plinth cells with the wrong ground tile. Fix: gate the skip to `meta["layout_strategy"] == "mansion_enclosure"`. Courtyard now regenerates **byte-identical to baseline (d948124^)** and mansion still 6/6.
- [x] 6.2 Add an orientation regression test: for each mansion archetype, `facing=south` (default) produces byte-identical output to the pre-change baseline (proves the `wall` param is additive).
      - **Implemented**: `tools/buildgen/tests/test_mansion_orientation_regression.py` + `fixtures/mansion_orientation_baseline_hashes.txt`. Baseline SHA-256 captured at `d948124^` and verified byte-identical to the current tree for `gate_house/front_row/open_hall/side_wing/tower_house` (config: seed 20260618, roof `chinese_round_ridge`, `group_id=None`). Test also asserts explicit `facing="south"` == default. Passes.
- [x] 6.3 Run the full validation checklist from `docs/ai-kb/09_validation_checklist.md` (generate, validate incl. voxel-walkability on all families, preview, build jar).
      - **Run**: `generate_all_structures` succeeded (112 NBTs). `validate_compound_library` courtyard 6/6, `chinese_mansion` 6/6, town 6/6, sect 2/2, and civic 8/8 all pass. `validate_mod_block_fallbacks` / `validate_plaque_bindings` / `validate_town_generation` / `validate_runtime_town_plan` / `validate_sect_generation` / `validate_region_topology` / `check_style_policy` / `check_cultivation_forms` pass. `preview_structure.py --all`, town/sect/region previews, and aggregate `out/preview/index.html` generated. `./gradlew build` → **BUILD SUCCESSFUL**. `validate_generated_structures` still has 1 known unrelated heuristic error — `hero_rockery.nbt: key_building_blocks_missing` (the 假山 fragment is not a building).

## 7. Library regeneration + reports

- [x] 7.1 Regenerate `src/main/resources/data/myvillage/structure/chinese_mansion_001..006.nbt` via `generate_compound_library.py --group chinese_mansion --count 6 --base-seed 20260618 --profile vanilla` (and `--profile full` for the shipped tree).
      - **Run**: vanilla proof and full shipped profile both succeeded. Updated `src/main/resources/data/myvillage/structure/chinese_mansion_001..006.nbt` (plus the mansion gallery/place functions as emitted by the generator).
- [x] 7.2 Regenerate `reports/compound_library_report.json` and `reports/compound_library_validation.json` mansion entries (new `facing_per_slot`, `door_reachable_rate=1.0`). Confirm `chinese_courtyard_*` entries unchanged.
      - **Run**: mansion report paths are group-specific (`reports/chinese_mansion_compound_library_report.json` and `reports/chinese_mansion_compound_library_validation.json`, ignored deterministic outputs). `validate_compound_library.py --group chinese_mansion --count 6 --profile full` passed 6/6; `door_reachable_rate=1.0` and `facing_per_slot` present for every mansion.
- [x] 7.3 Confirm the 6 mansions remain visibly distinct (silhouette spread ≥ 15) AND now each pass the enclosure invariants (gate_house, form-rule facings, every door on path).
      - **Confirmed**: `distinct_variants=6`, `silhouette_spread=29`, report `passed=true`, and every `validate_mansion` entry passed the gate_house / facing / path invariants.

## 8. Specs and docs

- [x] 8.1 Specs in this change (`compound-enclosure-planning`, `building-orientation-variants`, `mansion-gate-house` new; `chinese-mansion-compound`, `validation` delta) — already drafted in this proposal.
      - **Validated**: fixed delta headers / first-line SHALL parsing for the new and modified requirements; `openspec validate rebuild-mansion-enclosure-plan --strict` now passes.
- [x] 8.2 Update `docs/ai-kb/10_civic_family.md`: rewrite the 江南大宅 section to the enclosure model; add a "compound planning skeleton" note (enclosure model + facing variants; band model retained for `chinese_courtyard` pending propagation).
      - **Updated**: mansion section now documents the enclosure skeleton, door-wall facing variants, gate_house through-building, path-as-input, new validator stats, and see-also links to this change's specs.
- [x] 8.3 Update `docs/ai-kb/14_deferred_roadmap.md`: add an entry "propagate enclosure + orientation skeleton to `chinese_courtyard` + `small_courtyard`" as the immediate next change.
      - **Updated**: added E.4 for propagating the enclosure + orientation skeleton to `chinese_courtyard` and embedded `small_courtyard`.
- [x] 8.4 Update `AGENTS.md`: replace the band-model mansion paragraph with the enclosure-model paragraph; note the facing-variant mechanism and that `chinese_courtyard`/`small_courtyard` still use the old planner pending propagation.
      - **Updated**: mansion agent note now describes the enclosure model, gate_house, door-wall relocation facings, `validate_mansion` invariants, and the deferred courtyard propagation.
- [x] 8.5 Update `README.md`: command list unchanged (same `/place chinese_mansion_*` ids); the in-game form change is noted in CHANGELOG, not README commands.
      - **Updated**: README notes the v0.17.0 mansion form change while keeping `/myvillage place chinese_mansion_001..006` unchanged; jar examples now use `myvillage-0.17.0.jar`.

## 9. Version bump + acceptance

- [x] 9.1 Bump the mod version per `openspec/config.yaml` `rules.tasks` — large-feature bump `0.16.2-fix1 → 0.17.0` (new planning skeleton + 3 new capabilities + mansion NBT regeneration), updating `gradle.properties`, `src/main/resources/META-INF/neoforge.mods.toml`, README jar-name examples, and `CHANGELOG.md` together. Note the mansion enclosure-model rewrite and the (unchanged) other families in `CHANGELOG.md`.
      - **Updated**: `gradle.properties`, `src/main/resources/META-INF/neoforge.mods.toml`, README jar examples, and `CHANGELOG.md` now use / document `0.17.0`.
- [ ] 9.2 Build the mod jar; confirm `chinese_mansion_001..006` load and place in-game.
      - **Build complete**: `./gradlew build` succeeded and produced `build/libs/myvillage-0.17.0.jar`. Jar checks confirmed `data/myvillage/structure/chinese_mansion_001.nbt`, `data/myvillage/mod_block_fallbacks.json`, plaque blockstates/textures, and inscription painting data/textures are packaged. **Not checked in-game in this environment**; leave unchecked until a Minecraft client places the structures.
- [ ] 9.3 Staged manual acceptance: place ≥2 `chinese_mansion_*` NBTs in-game and confirm (a) the entrance reads as a real gate (门楼 + 门框 + through-passage, not a hole), (b) every building's door faces its yard (倒座 door toward 前院, 厢 doors inward, 正房 south), (c) the gravel path reaches every door, (d) the player walks end-to-end gate → 正房 → 楼阁 with no dead ends. Capture screenshots; serve the preview aggregate over HTTP per `AGENTS.md` and report the host URL.
      - **Prepared for manual review**: aggregate preview exists at `out/preview/index.html`; HTTP server running on port 8766 for review. Needs in-game placement + screenshots before checking.
- [ ] 9.4 Staged manual acceptance: place the remaining mansion variants and confirm the 6 read as visibly distinct (plan / roofline / 花园 scale) AND all share the corrected entrance + facing + path form. Capture screenshots.
      - **Prepared for manual review**: all six `chinese_mansion_*` NBT previews were generated, and report validation confirms `distinct_variants=6`, `silhouette_spread=29`, `door_reachable_rate=1.0`. Needs in-game visual placement + screenshots before checking.
