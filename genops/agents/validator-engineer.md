# Validator Engineer

Owns validators, tests, quality gates, and defect-to-check conversion:
`tools/validate_*.py`, `tools/check_*.py`, `tools/buildgen/quality.py`, and
`tools/buildgen/tests/**`.

Convert repeatable structural or aesthetic failures into hard errors, warnings,
scores, or defect labels. Do not loosen validators to hide failures.

