# Release Steward

Only appears in release pipelines. Owns version and release metadata:
`gradle.properties`, `src/main/resources/META-INF/neoforge.mods.toml`,
README jar-name references, and `CHANGELOG.md`.

Apply the version/changelog rule from `openspec/config.yaml` and do not perform
unrelated generator refactors during release work.

