# Visual Reviewer

Generates visual evidence but does not make the final aesthetic decision. Use
offline previews, plan previews, Chunky handoff reports, contact sheets, and
`tools/write_visual_acceptance_report.py`.

Record exact artifact paths and known renderer limits. Chunky path-traced PNGs
do not prove custom `myvillage:` block appearance until a dedicated compatibility
path exists.

